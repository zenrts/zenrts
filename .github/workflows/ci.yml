name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-latest, windows-latest]
        compiler: [gcc, clang]
        build_type: [Debug, Release]
        exclude:
          - os: macos-latest
            compiler: gcc
          - os: windows-latest
            compiler: gcc
          - os: windows-latest
            compiler: clang
        include:
          - os: windows-latest
            compiler: msvc
            build_type: Debug
            shell: pwsh
          - os: windows-latest
            compiler: msvc
            build_type: Release
            shell: pwsh

    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: ${{ matrix.shell || 'bash' }}

    steps:
      - uses: actions/checkout@v5

      - name: Setup compiler (GCC/Clang)
        if: matrix.compiler != 'msvc'
        run: |
          echo "CC=${{ matrix.compiler == 'gcc' && 'gcc' || 'clang' }}" >> $GITHUB_ENV
          echo "CXX=${{ matrix.compiler == 'gcc' && 'g++' || 'clang++' }}" >> $GITHUB_ENV

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -DZENRTS_BUILD_TESTS=ON -DZENRTS_BUILD_BENCHMARKS=ON

      - name: Build
        run: cmake --build build --config ${{ matrix.build_type }} --parallel

      - name: Test
        run: ctest --test-dir build --output-on-failure -C ${{ matrix.build_type }} --parallel

      - name: Benchmark (Unix)
        if: runner.os != 'Windows'
        run: ./build/benchmarks/benchmark_zenrts

  sanitizers:
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
        sanitizer:
          - name: ASAN
            flags: -fsanitize=address -fno-omit-frame-pointer
          - name: UBSAN
            flags: -fsanitize=undefined -fno-omit-frame-pointer
          - name: TSAN
            flags: -fsanitize=thread -fno-omit-frame-pointer

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Setup compiler
        run: |
          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            echo "CC=gcc" >> $GITHUB_ENV
            echo "CXX=g++" >> $GITHUB_ENV
          else
            echo "CC=clang" >> $GITHUB_ENV
            echo "CXX=clang++" >> $GITHUB_ENV
          fi

      - name: Configure
        run: >
          cmake -B build
          -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_CXX_FLAGS="${{ matrix.sanitizer.flags }}"
          -DCMAKE_EXE_LINKER_FLAGS="${{ matrix.sanitizer.flags }}"
          -DZENRTS_BUILD_TESTS=ON
          -DZENRTS_BUILD_BENCHMARKS=OFF

      - name: Build
        run: cmake --build build --parallel --config Debug

      - name: Test
        run: ctest --test-dir build --output-on-failure --parallel -C Debug

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: linux-amd64
            compiler: gcc
            cc: gcc
            cxx: g++
          - os: ubuntu-24.04-arm
            arch: linux-arm64
            compiler: gcc
            cc: gcc
            cxx: g++
          - os: macos-latest
            arch: macos-arm64
            compiler: clang
            cc: clang
            cxx: clang++
          - os: windows-latest
            arch: windows-amd64
            compiler: msvc
            shell: pwsh

    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: ${{ matrix.shell || 'bash' }}

    steps:
      - uses: actions/checkout@v5

      - name: Setup compiler (Unix)
        if: runner.os != 'Windows'
        run: |
          echo "CC=${{ matrix.cc }}" >> $GITHUB_ENV
          echo "CXX=${{ matrix.cxx }}" >> $GITHUB_ENV

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DZENRTS_BUILD_TESTS=OFF -DZENRTS_BUILD_BENCHMARKS=OFF

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Install
        run: cmake --install build --config Release --prefix ${{ runner.temp }}/zenrts-${{ matrix.arch }}

      - name: Package
        run: tar -C ${{ runner.temp }} -czf ${{ runner.temp }}/zenrts-${{ matrix.arch }}.tar.gz zenrts-${{ matrix.arch }}

      - name: Upload
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ runner.temp }}/zenrts-${{ matrix.arch }}.tar.gz
